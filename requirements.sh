#!/usr/bin/env bash

sudo npm install hexo --save
sudo npm install hexo-server --save

sudo npm install gulp --save
sudo npm install gulp-minify-css --save
sudo npm install gulp-minify-html --save
sudo npm install gulp-uglify --save
sudo npm install gulp-rename --save
sudo npm install gulp-clean --save
sudo npm install gulp-shell --save
